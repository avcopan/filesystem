name: filesystem-env

channels:
    - pacchem
    - psi4       # for elstruct dependency
    - rdkit      # for autochem dependency
    - openbabel  # for autochem dependency

dependencies:
    - python
    - numpy
    - pyyaml
    - pytest
    - pytest-cov
    - future
    - flake8
    - pylint
    - pacchem::autochem
    - pacchem::elstruct
